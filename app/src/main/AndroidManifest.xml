<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muchlis.inventaris">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".views.activity.AppendComputerActivity"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity android:name=".views.activity.AppendHistoryActivity" />
        <activity android:name=".views.activity.ComputerDetailActivity" />
        <activity android:name=".views.activity.ComputersActivity" />
        <activity android:name=".views.activity.DashboardActivity" />
        <activity android:name=".views.activity.LoginActivity" />
        <activity android:name=".MainEmptyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>